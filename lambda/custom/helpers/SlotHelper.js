
/**
 * Returns an array of objects {slotName, value, failType} with any invalid slots
 * @param {object} slotObject 
 * @param {array} slotNamesToCheck 
 */
exports.checkSlots = (slotObject, slotNamesToCheck) => {
    
};